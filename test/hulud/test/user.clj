(ns hulud.test.user
  (:use clojure.test)
  (:require [hulud.util :as util]
            [hulud.models.db :as db]))

;tests
(deftest test-user
;  (testing "saveing a user"
;    (let [result (db/save-user "bob" "alice" "franz@kafka.de")]
;      (is (= 1 (first (vals result))))))
;  
;  (testing "get-user-by-name"
;    (let [user (db/get-user-by-name "bob")]
;      (is (= "bob" (:name user)))))
;
;  (testing "correct-password-for-user?"
;     (let [result (db/correct-password-for-user? "bob" "alice")]
;       (is (result)))
;     (let [result (db/correct-password-for-user? "bob" "franz")]
;       (is (not (result))))))
         )
